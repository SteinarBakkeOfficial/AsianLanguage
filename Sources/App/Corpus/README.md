# Corpus

Bundled read-only Shared Character loading. Authoring JSON lives under content/shared-characters; Resources/Corpus is generated runtime data.

Runtime records use readable, meaning-based IDs and filenames (for example, `one.json`); collision cases use a deterministic Unicode suffix. Research/source folders retain their Unicode-based names because they are archival evidence paths rather than app-facing record names.
