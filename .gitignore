# Local production and research material. Keep these folders available locally,
# but never treat them as part of the distributable app/project package.
/artwork_production/
/Reference Pictures/
/content/archive/
/docs/archive/

# A final-project export is generated from an explicit allowlist and is disposable.
/artifacts/final-project/
